---
tls:
  certificates:
    - certFile: /certs/{{ openclaw_service_domain }}.crt
      keyFile: /certs/{{ openclaw_service_domain }}.pem
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: true
